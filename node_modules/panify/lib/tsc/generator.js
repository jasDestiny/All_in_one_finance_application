"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Generator = void 0;
const luhn_1 = require("@amm834/luhn");
const randomRng_1 = require("./randomRng");
class Generator {
    constructor(BIN, LIMIT = 10) {
        this.BIN = BIN;
        this.LIMIT = LIMIT;
        this.BIN = BIN;
        this.LIMIT = LIMIT;
    }
    generate() {
        let panAry = [];
        while (true) {
            const generatedPan = this.BIN + '' + randomRng_1.randRng(0, 9999999999);
            if (luhn_1.Luhn.validate(parseInt(generatedPan))) {
                const panObject = {
                    card_number: parseInt(generatedPan),
                    month: randomRng_1.randDate().month,
                    year: randomRng_1.randDate().year
                };
                panAry.push(panObject);
            }
            if (panAry.length === this.LIMIT) {
                return panAry;
            }
        }
    }
}
exports.Generator = Generator;
//# sourceMappingURL=generator.js.map